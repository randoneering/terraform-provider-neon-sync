---
page_title: "neon_bucket Resource - terraform-provider-neon"
description: |-
  Creates and manages a branchable object storage bucket on a Neon branch.
---

# neon_bucket (Resource)

Creates and manages a branchable object storage bucket on a Neon branch.

## Example Usage

```terraform
resource "neon_project" "example" {
  name      = "foo"
  region_id = "aws-us-east-2"
}

# the bucket is private by default
resource "neon_bucket" "private" {
  project_id = neon_project.example.id
  branch_id  = neon_project.example.default_branch_id
  name       = "foo"
}

# set the access level to allow for public read operations
resource "neon_bucket" "public" {
  project_id   = neon_project.example.id
  branch_id    = neon_project.example.default_branch_id
  name         = "bar"
  access_level = "public_read"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `branch_id` (String) The Neon branch ID
- `name` (String) The bucket name.
- `project_id` (String) The Neon project ID

### Optional

- `access_level` (String) Access level for the bucket. Defaults to `private`. Set to `public_read` to allow anonymous `GetObject`/`HeadObject` on objects in this bucket.

### Read-Only

- `id` (String) The bucket resource ID.
- `region` (String) The AWS region for this branch's object storage.
- `s3_endpoint` (String) The S3-compatible endpoint URL for this branch.



## Import

The Neon Bucket can be imported to the terraform state by its composite identifier that consists of
`ProjectID`, `BranchID` and the `BucketName` separated by a forward slash.

Import using the [import block](https://developer.hashicorp.com/terraform/language/import):

For example:

```hcl
import {
  to = neon_bucket.example
  id = "curly-poetry-30604233/br-snowy-mountain-a5jkb18i/my-bucket"
}
```

Import using the command `terraform import`:

```commandline
terraform import neon_bucket.example "curly-poetry-30604233/br-snowy-mountain-a5jkb18i/my-bucket"
```
